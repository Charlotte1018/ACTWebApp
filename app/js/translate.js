angular.module("angularTranslateModule", [])
    .config(function ($translateProvider) {
        $translateProvider
            .translations('zh', {
                
            })
            .translations('en', {
                
            })
            .translations('ja', {
                
            });
        $translateProvider.preferredLanguage('zh');

    })
